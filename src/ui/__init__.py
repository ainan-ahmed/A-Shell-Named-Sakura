"""
UI package containing Blueprint (.blp) UI definition files.

Blueprint files are compiled to .ui (GtkBuilder XML) files during build
and loaded via GResource by the corresponding window/widget classes.
"""
